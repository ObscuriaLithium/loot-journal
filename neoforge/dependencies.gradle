dependencies {
    implementation("dev.obscuria:fragmentum-neoforge:${minecraft_version}-${fragmentum_version}")
    implementation("dev.isxander:yet-another-config-lib:3.8.2+${minecraft_version}-neoforge")
}